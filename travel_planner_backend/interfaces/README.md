This folder contains the generated OpenAPI specification for the backend.

To regenerate:
1. cd travel_planner_backend
2. python generate_openapi.py

The file openapi.json will then reflect the current routes and schemas.
